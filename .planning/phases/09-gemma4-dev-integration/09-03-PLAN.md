---
wave: 2
depends_on: [09-01]
files_modified: [src/lib/parsers.ts]
autonomous: true
---

# Plan 09-03: File Parsers Implementation

Implements the deterministic text extraction logic for various project document formats.

## Proposed Changes

### Utils
- Create `src/lib/parsers.ts`.
- Implement `parseXLSX(buffer: Buffer)`.
- Implement `parseDOCX(buffer: Buffer)`.
- Implement `parseCSV(buffer: Buffer)`.

## Tasks

<task>
<read_first>
- package.json
</read_first>
<action>
Implement `parseXLSX` in `src/lib/parsers.ts` using the `xlsx` library.
Convert sheets to Markdown tables for better LLM context.
</action>
<acceptance_criteria>
- parseXLSX extracts data from a buffer and returns string.
</acceptance_criteria>
</task>

<task>
<read_first>
- package.json
</read_first>
<action>
Implement `parseDOCX` using `mammoth.extractRawText`.
</action>
<acceptance_criteria>
- parseDOCX returns plain text from a Word document.
</acceptance_criteria>
</task>

<task>
<read_first>
- package.json
</read_first>
<action>
Implement `parseCSV` using `papaparse`.
</action>
<acceptance_criteria>
- parseCSV returns a formatted string or JSON representation.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/app/api/extract/route.ts
</read_first>
<action>
Integrate parsers into the extraction API route.
</action>
<acceptance_criteria>
- API route successfully extracts text before calling LLM.
</acceptance_criteria>
</task>

## Verification Plans

### Automated Tests
- Create `src/lib/parsers.test.ts` with small file buffers.
- Run `npm test` (if configured) or simple node script to verify output.

## must_haves
- [ ] `src/lib/parsers.ts` successfully parses DOCX, CSV, and XLSX.
- [ ] API route integrates these parsers to extract text.
