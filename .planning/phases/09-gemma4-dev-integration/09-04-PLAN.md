---
wave: 3
depends_on: [09-02, 09-03]
files_modified: [src/components/modals/DataImportModal.tsx, src/hooks/use-extractions.ts]
autonomous: true
---

# Plan 09-04: UI Integration & Review Flow

Creates the user interface for uploading files, monitoring AI extraction, and reviewing/committing the results.

## Proposed Changes

### Hooks
- Create `src/hooks/use-extractions.ts` using `@tanstack/react-query`.
- Implement `useExtraction(id)` with pooling (`refetchInterval`) when status is `processing`.

### Components
- Create `src/components/modals/DataImportModal.tsx`:
  - Step 1: File Dropzone.
  - Step 2: Processing state (Progress indicator).
  - Step 3: Review Table (Editable fields).
  - Step 4: Success/Commit message.

### Integration
- Add "Import via AI" button to the Projects page or Sidebar.

## Tasks

<task>
<read_first>
- src/hooks/use-tasks.ts
</read_first>
<action>
Create `src/hooks/use-extractions.ts`.
Implement `useExtractionStatus` and `useStartExtraction` mutation.
Ensure pooling is enabled for active jobs.
</action>
<acceptance_criteria>
- hook correctly fetches data from Supabase `extractions`.
- `refetchInterval` works as expected.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/components/ui/button.tsx
- src/components/FileDropzone.tsx
</read_first>
<action>
Implement `DataImportModal` with multi-step logic.
Use `ReviewTable.tsx` (inner component) to display the extracted JSONB data.
</action>
<acceptance_criteria>
- Modal opens and handles the transition between Upload -> Processing -> Review.
</acceptance_criteria>
</task>

<task>
<read_first>
- src/lib/db/projects.ts
</read_first>
<action>
Implement the "Commit" logic in the modal:
- Send the confirmed/edited project data to `createProject`.
</action>
<acceptance_criteria>
- Clicking "Commit" creates a real project in the database.
</acceptance_criteria>
</task>

## Verification Plans

### Manual Verification
1. Open the Import Modal.
2. Drop a project document.
3. Wait for LLM processing to complete.
4. Verify the "Review" table shows extracted fields (Name, Priority, etc.).
5. Edit one field and click "Commit".
6. Verify the project appears in the Dashboard.

## must_haves
- [ ] `useExtraction` hook polls for status changes.
- [ ] `DataImportModal` handles the 3-step flow (Upload, Process, Review).
- [ ] Extracted projects can be successfully committed to the database.
