---
phase: 08-supabase-data-layer-integration
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - src/lib/db/projects.ts
  - src/lib/db/tasks.ts
  - src/hooks/use-projects.ts
  - src/hooks/use-tasks.ts
autonomous: true
requirements: [DB-03]

must_haves:
  truths:
    - "Projects and Tasks CRUD methods are implemented"
    - "React Query hooks handle caching and invalidated properly"
  artifacts:
    - path: "src/lib/db/projects.ts"
      provides: "Project CRUD operations"
    - path: "src/hooks/use-projects.ts"
      provides: "Query/Mutation hooks for projects"
---

<objective>
Implement the data access layer and React Query hooks for Projects and Tasks.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Project Data Access Layer</name>
  <files>src/lib/db/projects.ts, src/hooks/use-projects.ts</files>
  <action>
1. Create `src/lib/db/projects.ts` with `getProjects`, `createProject`, `updateProject`, `deleteProject`.
2. Create `src/hooks/use-projects.ts` with `useProjects` and `useCreateProject`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Task Data Access Layer</name>
  <files>src/lib/db/tasks.ts, src/hooks/use-tasks.ts</files>
  <action>
1. Create `src/lib/db/tasks.ts` with `getTasks`, `createTask`, `updateTask`, `deleteTask`.
2. Create `src/hooks/use-tasks.ts` with `useTasks` and `useCreateTask`.
  </action>
</task>

</tasks>

<success_criteria>
- Hooks implemented and typed correctly.
- Basic CRUD operations functional.
</success_criteria>
