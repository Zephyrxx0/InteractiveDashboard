---
phase: 08-supabase-data-layer-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/supabase.ts
  - src/lib/auth.tsx
  - .env.local
autonomous: true
requirements: [DB-01]

must_haves:
  truths:
    - "Supabase client is initialized"
    - "Auth context supports hardcoded admin/password123 login"
    - "Environment variables placeholders exist"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Supabase client singleton"
    - path: "src/lib/auth.tsx"
      provides: "Mock Auth provider with hardcoded credentials"
---

<objective>
Setup the foundational infrastructure for Supabase and hardcoded authentication.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies</name>
  <files>package.json</files>
  <action>
1. Install Supabase JS: `npm install @supabase/supabase-js @tanstack/react-query`
  </action>
</task>

<task type="auto">
  <name>Task 2: Initialize Supabase Client</name>
  <files>src/lib/supabase.ts, .env.local</files>
  <action>
1. Add placeholders to `.env.local`:
   ```bash
   NEXT_PUBLIC_SUPABASE_URL="https://your-instance.supabase.co"
   NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
   ```
2. Create `src/lib/supabase.ts`:
   ```typescript
   import { createClient } from '@supabase/supabase-js';

   const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
   const supabaseAnonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

   export const supabase = createClient(supabaseUrl, supabaseAnonKey);
   ```
  </action>
</task>

<task type="auto">
  <name>Task 3: Implement Hardcoded Auth</name>
  <files>src/lib/auth.tsx</files>
  <action>
1. Modify (or wrap) `AuthProvider` to support a local `admin` / `password123` check.
2. If credentials match, set a mock user object with `uid: "admin-dev-01"`.
3. Keep the existing Firebase imports but comment out the real triggers for now.
  </action>
</task>

</tasks>

<success_criteria>
- Dependencies installed.
- Supabase client exported.
- Login works with admin/password123.
</success_criteria>
