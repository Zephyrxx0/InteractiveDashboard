---
phase: 08-supabase-data-layer-integration
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified: []
autonomous: true
requirements: [DB-02]

must_haves:
  truths:
    - "SQL schema covers all scouted entities"
    - "TypeScript types match the DB schema"
  artifacts:
    - path: "sql/schema.sql"
      provides: "Full database schema for Supabase"
---

<objective>
Generate the SQL schema for all project entities and define the resulting TypeScript types.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Generate SQL Schema File</name>
  <files>sql/schema.sql</files>
  <action>
1. Create a `sql/schema.sql` file containing all the tables (Projects, Tasks, Tags, Dependencies, Media, Extractions, Notifications) as scouted.
2. Include indexes for foreign keys and common query filters.
  </action>
</task>

<task type="auto">
  <name>Task 2: Define Database Types</name>
  <files>src/types/database.ts</files>
  <action>
1. Translate the SQL schema into TypeScript interfaces.
2. Use these types in the upcoming data hooks.
  </action>
</task>

</tasks>

<success_criteria>
- Schema file created.
- Database types defined and consistent with existing UI types.
</success_criteria>
