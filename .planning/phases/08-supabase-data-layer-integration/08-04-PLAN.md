---
phase: 08-supabase-data-layer-integration
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - src/app/(dashboard)/tasks/page.tsx
  - src/app/(dashboard)/projects/[id]/tasks/page.tsx
autonomous: true
requirements: [DB-04]

must_haves:
  truths:
    - "Tasks Hub displays data from Supabase"
    - "Manual data entry or 'Add Task' works"
  artifacts:
    - path: "src/app/(dashboard)/tasks/page.tsx"
      provides: "Sync tasks list UI"
---

<objective>
Replace mock data with real Supabase data in the Tasks Hub and Project detail pages.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Tasks Hub</name>
  <files>src/app/(dashboard)/tasks/page.tsx</files>
  <action>
1. Replace `MOCK_TASKS` with `useTasks()` hook.
2. Update the `addTask` handler to use `useMutation` from React Query.
  </action>
</task>

<task type="auto">
  <name>Task 2: Migrate Project Detail Tasks</name>
  <files>src/app/(dashboard)/projects/[id]/tasks/page.tsx</files>
  <action>
1. Replace mock data with project-filtered `useTasks({ projectId: id })`.
  </action>
</task>

</tasks>

<success_criteria>
- Real data visible in the UI.
- CRUD operations reflect in the database.
</success_criteria>
