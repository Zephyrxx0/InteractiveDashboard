---
phase: 01-analytics-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/ui/kpi-card.tsx
  - src/components/ui/date-range-picker.tsx
  - src/app/(dashboard)/analytics/page.tsx
autonomous: true
requirements: [AN-01, AN-05]

must_haves:
  truths:
    - "KPI cards display trend indicators (up/down arrows with color)"
    - "User can select date range (last 7 days, 30 days, custom)"
    - "Analytics page shows KPI cards with real layout"
  artifacts:
    - path: "src/components/ui/date-range-picker.tsx"
      provides: "Date range selector component"
      exports: ["DateRangePicker"]
    - path: "src/app/(dashboard)/analytics/page.tsx"
      provides: "Analytics page with KPIs and charts"
      min_lines: 50
  key_links:
    - from: "src/app/(dashboard)/analytics/page.tsx"
      to: "src/components/ui/charts/*"
      via: "component import"
      pattern: "import.*from.*charts"
---

<objective>
Enhance KPI cards with trend indicators and create date range filtering for the analytics page.

Purpose: Build the analytics dashboard UI that users will interact with, integrating chart components from Plan 01.
Output: Enhanced KPI cards, date range picker, and populated analytics page
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-analytics-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Existing KPI card interface -->
From src/components/ui/kpi-card.tsx:
```typescript
interface KpiCardProps {
    title: string;
    value: string;
    icon: string;
    trend?: {
        value: string;
        direction: "up" | "down" | "stable";
        label: string;
    };
    children?: React.ReactNode;
    className?: string;
}
```

<!-- Chart components from Plan 01 -->
From src/components/ui/charts/index.ts:
```typescript
export { LineChartWidget } from './line-chart';
export { BarChartWidget } from './bar-chart';
export { PieChartWidget } from './pie-chart';
```

<!-- Date picker already exists -->
From src/components/ui/calendar.tsx:
```typescript
// Uses react-day-picker 9.14.0
// Can be composed for date range
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DateRangePicker component</name>
  <files>src/components/ui/date-range-picker.tsx</files>
  <action>
Create a date range picker component using existing calendar.tsx and popover.tsx:

```tsx
'use client';

interface DateRangePickerProps {
  value?: { from: Date; to: Date };
  onChange: (range: { from: Date; to: Date } | undefined) => void;
  presets?: Array<{ label: string; days: number }>; // e.g., "Last 7 days"
  className?: string;
}
```

Features:
1. Preset buttons for quick selection: "Last 7 days", "Last 30 days", "Last 90 days"
2. Custom date range via calendar popover
3. Display selected range as "Mar 1 - Mar 27, 2026" format
4. Use date-fns (already in stack at 4.1.0) for formatting
5. Follow existing UI patterns (font-mono for dates, border-border, etc.)
6. Trigger button shows calendar icon and current range text

Use existing components:
- Popover from ui/popover.tsx
- Calendar from ui/calendar.tsx (wraps react-day-picker)
- Button from ui/button.tsx
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/ui/date-range-picker.tsx</automated>
  </verify>
  <done>DateRangePicker component exists with presets and custom range selection</done>
</task>

<task type="auto">
  <name>Task 2: Build analytics page with charts and KPIs</name>
  <files>src/app/(dashboard)/analytics/page.tsx</files>
  <action>
Replace the empty state in analytics page with a full dashboard layout:

1. **Header section:**
   - Use existing PageHeader component
   - Add DateRangePicker in actions slot
   - Title: "Global Analytics", Subtitle: "High-level insights across all projects"

2. **KPI row (4 cards):**
   - Use existing KpiCard component
   - Mock data for: "Total Tasks", "Completed", "In Progress", "Overdue"
   - Each with trend indicators (up/down/stable)

3. **Chart row:**
   - LineChartWidget: "Task Completion Trend" (tasks completed over time)
   - BarChartWidget: "Tasks by Assignee" (comparison)
   - PieChartWidget: "Task Status Distribution" (todo/in-progress/done/blocked)

4. **Grid layout:**
   - Follow existing dashboard page pattern
   - Responsive: 1 col mobile, 2 col tablet, 4 col desktop for KPIs
   - Charts: 2 cols each on desktop

5. **Mock data:**
   - Create realistic mock data arrays for charts
   - Use last 7 days for line chart
   - Use assignee names for bar chart
   - Use status labels for pie chart

Pattern from existing dashboard page:
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 xl:grid-cols-12 gap-4">
```
  </action>
  <verify>
    <automated>npm run build && curl -s http://localhost:3000/analytics 2>/dev/null || echo "Build passed"</automated>
  </verify>
  <done>Analytics page displays KPI cards, line chart, bar chart, and pie chart</done>
</task>

</tasks>

<verification>
1. Navigate to /analytics - page loads without errors
2. KPI cards show trend indicators with correct colors
3. DateRangePicker shows presets and allows custom selection
4. All three chart types render with mock data
5. Page is responsive on mobile/tablet/desktop
</verification>

<success_criteria>
- Analytics page no longer shows empty state
- Four KPI cards with trend indicators visible
- DateRangePicker allows selecting date ranges
- Line, Bar, and Pie charts render with mock data
- No console errors or hydration warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-analytics-foundation/01-02-SUMMARY.md`
</output>
