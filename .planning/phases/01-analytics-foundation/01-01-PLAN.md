---
phase: 01-analytics-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/charts/index.ts
  - src/components/ui/charts/line-chart.tsx
  - src/components/ui/charts/bar-chart.tsx
  - src/components/ui/charts/pie-chart.tsx
  - src/components/ui/charts/chart-container.tsx
autonomous: true
requirements: [AN-01, AN-02, AN-03, AN-04]

must_haves:
  truths:
    - "Charts render without SSR hydration errors"
    - "Line chart displays trend data over time"
    - "Bar chart displays categorical comparison"
    - "Pie/donut chart displays distribution data"
  artifacts:
    - path: "src/components/ui/charts/line-chart.tsx"
      provides: "Recharts-based line chart wrapper"
      exports: ["LineChartWidget"]
    - path: "src/components/ui/charts/bar-chart.tsx"
      provides: "Recharts-based bar chart wrapper"
      exports: ["BarChartWidget"]
    - path: "src/components/ui/charts/pie-chart.tsx"
      provides: "Recharts-based pie chart wrapper"
      exports: ["PieChartWidget"]
  key_links:
    - from: "src/components/ui/charts/*.tsx"
      to: "recharts"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*ssr.*false"
---

<objective>
Install Recharts library and create SSR-safe chart wrapper components that follow existing UI patterns.

Purpose: Establish chart infrastructure that works with Next.js App Router SSR, avoiding hydration errors (Pitfall #1 from research).
Output: Three reusable chart components (Line, Bar, Pie) in src/components/ui/charts/
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Existing UI component pattern from button.tsx -->
From src/components/ui/button.tsx:
```typescript
import { cn } from "@/lib/utils";
import { cva, type VariantProps } from "class-variance-authority";
// Pattern: Use cva for variants, cn for class merging
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and create chart directory</name>
  <files>package.json, src/components/ui/charts/index.ts</files>
  <action>
1. Install recharts: `npm install recharts`
2. Create directory: `src/components/ui/charts/`
3. Create barrel export file `index.ts` with placeholder exports

The charts directory follows the existing `ui/` pattern where primitives live.
  </action>
  <verify>
    <automated>npm ls recharts && test -f src/components/ui/charts/index.ts</automated>
  </verify>
  <done>Recharts installed, charts directory exists with index.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SSR-safe chart container wrapper</name>
  <files>src/components/ui/charts/chart-container.tsx</files>
  <behavior>
    - ChartContainer renders children only on client (no SSR)
    - Shows loading skeleton while chart loads
    - Accepts className for styling
    - Handles ResponsiveContainer from Recharts
  </behavior>
  <action>
Create `chart-container.tsx` that:
1. Uses `'use client'` directive at top
2. Uses `dynamic` import pattern from next/dynamic with `ssr: false` for heavy chart components
3. Wraps content in Recharts' ResponsiveContainer for responsive sizing
4. Shows Skeleton component during loading (import from existing ui/skeleton.tsx)
5. Accepts `className`, `height` (default 300), and `children` props

Pattern:
```tsx
'use client';
import dynamic from 'next/dynamic';
import { Skeleton } from '@/components/ui/skeleton';
import { cn } from '@/lib/utils';

// This wrapper ensures charts only render client-side
```

CRITICAL: Do NOT import Recharts components directly at module level. Use dynamic imports or ensure all chart components have 'use client' and are loaded via next/dynamic in pages.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/ui/charts/chart-container.tsx</automated>
  </verify>
  <done>ChartContainer component renders without SSR errors</done>
</task>

<task type="auto">
  <name>Task 3: Create Line, Bar, and Pie chart components</name>
  <files>src/components/ui/charts/line-chart.tsx, src/components/ui/charts/bar-chart.tsx, src/components/ui/charts/pie-chart.tsx, src/components/ui/charts/index.ts</files>
  <action>
Create three chart wrapper components following existing UI patterns:

**LineChartWidget (line-chart.tsx):**
```tsx
'use client';
import { LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid } from 'recharts';

interface LineChartWidgetProps {
  data: Array<{ [key: string]: string | number }>;
  dataKey: string;
  xAxisKey: string;
  title?: string;
  subtitle?: string;
  className?: string;
  height?: number;
  color?: string; // Default to primary color
}
```

**BarChartWidget (bar-chart.tsx):**
```tsx
'use client';
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid } from 'recharts';

interface BarChartWidgetProps {
  data: Array<{ [key: string]: string | number }>;
  dataKey: string;
  xAxisKey: string;
  title?: string;
  subtitle?: string;
  className?: string;
  height?: number;
  color?: string;
}
```

**PieChartWidget (pie-chart.tsx):**
```tsx
'use client';
import { PieChart, Pie, Cell, Tooltip, ResponsiveContainer, Legend } from 'recharts';

interface PieChartWidgetProps {
  data: Array<{ name: string; value: number; color?: string }>;
  title?: string;
  subtitle?: string;
  className?: string;
  height?: number;
  innerRadius?: number; // For donut style
  outerRadius?: number;
}
```

All components should:
1. Use 'use client' directive
2. Follow existing card styling pattern from chart-widget.tsx (bg-card border border-border p-6)
3. Include title/subtitle header like existing widgets
4. Use cn() for className merging
5. Use CSS variables for colors where possible (--primary, etc.)

Update index.ts to export all components.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/ui/charts/*.tsx && npm run build</automated>
  </verify>
  <done>Three chart components exist, TypeScript compiles, build succeeds</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors (SSR compatibility)
2. Chart components can be imported: `import { LineChartWidget, BarChartWidget, PieChartWidget } from '@/components/ui/charts'`
3. No "window is not defined" or hydration errors in console
</verification>

<success_criteria>
- Recharts installed and listed in package.json dependencies
- Three chart components (Line, Bar, Pie) in src/components/ui/charts/
- All components use 'use client' directive
- TypeScript compiles without errors
- Next.js build completes (proves SSR safety)
</success_criteria>

<output>
After completion, create `.planning/phases/01-analytics-foundation/01-01-SUMMARY.md`
</output>
