---
wave: 1
depends_on: []
files_modified: [src/lib/extraction-schema.ts, src/app/api/extract/route.ts, feat_guide/IMPORT_GUIDE.md]
autonomous: true
---

# Plan 07-01: Document Imports Validation & AI Wiring

This plan implements the actual LLM-powered extraction logic and validates it against the project's historical data samples.

## Proposed Changes

### Extraction Logic
- Implement `ollama.chat` with a structured system prompt in `src/app/api/extract/route.ts`.
- Extract: `Project Name`, `Budget`, `Region`, `Timeline`, and `Main Objective`.

### Schema & Types
- Update `src/lib/extraction-schema.ts` to include the standard fields mentioned above.

### Documentation
- Create `feat_guide/IMPORT_GUIDE.md` as a technical reference for supported formats and expected extraction accuracy.

### Validation
- Create `src/lib/scripts/validate-imports.ts` (scratch script) to run the samples and log extraction results to `.planning/phases/07-document-imports-unstructured-files/07-VALIDATION-DATA.md`.

## Tasks

<task>
<action>
Modify `src/lib/extraction-schema.ts` to include NGO-specific fields.
</action>
<acceptance_criteria>
- `ProjectData` interface exists and is used in the extraction envelope.
</acceptance_criteria>
</task>

<task>
<action>
Implement the Ollama call in `src/app/api/extract/route.ts`.
</action>
<acceptance_criteria>
- Route no longer mocks the LLM response.
- System prompt is optimized for Gemma4.
</acceptance_criteria>
</task>

<task>
<action>
Create `feat_guide/IMPORT_GUIDE.md` and `07-VALIDATION-DATA.md`.
</action>
<acceptance_criteria>
- Documentation is clear and accurate.
- Validation data contains JSON references for the samples.
</acceptance_criteria>
</task>

## Verification Plans

### Manual Verification
1. Upload `feat_guide/YRA/MPPR Data folder/December 2023.xlsx`.
2. Verify the AI extracts the "December 2023" context correctly.
3. Commit and verify the project metadata.
