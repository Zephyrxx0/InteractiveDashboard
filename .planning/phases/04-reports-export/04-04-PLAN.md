---
phase: 04-reports-export
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - "src/types/report.ts"
  - "src/components/features/export/export-dialog.tsx"
  - "src/app/(dashboard)/reports/page.tsx"
autonomous: true
gap_closure: true
requirements: ["RP-03"]
must_haves:
  truths:
    - "User can select from summary or detailed report templates when exporting to PDF"
    - "PDF generation respects user template choice"
  artifacts:
    - path: "src/components/features/export/export-dialog.tsx"
      provides: "Template selector UI"
    - path: "src/app/(dashboard)/reports/page.tsx"
      provides: "Passes selected template to PDF exporter"
    - path: "src/types/report.ts"
      provides: "ExportOptions with template field"
  key_links:
    - from: "src/components/features/export/export-dialog.tsx"
      to: "src/app/(dashboard)/reports/page.tsx"
      via: "onExport callback with template option"
---

<objective>
Address verification gaps by adding a UI template selector for PDF exports.

Purpose: The PDF export utility supports different templates (summary and detailed), but the UI hardcodes 'summary'. This plan connects the UI to the existing PDF template capabilities, satisfying RP-03.
Output: Updated ExportDialog with template selector and ReportsPage passing the selection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reports-export/04-CONTEXT.md
@.planning/phases/04-reports-export/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add template to ExportOptions type</name>
  <read_first>
    - src/types/report.ts
  </read_first>
  <files>src/types/report.ts</files>
  <action>
    Modify `ExportOptions` interface in `src/types/report.ts` to include `template?: 'summary' | 'detailed';`.
    Leave all other properties in `ExportOptions` as they are.
  </action>
  <acceptance_criteria>
    - `grep "template?:" src/types/report.ts` returns a match
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ExportOptions includes template field</done>
</task>

<task type="auto">
  <name>Task 2: Add template selector to ExportDialog</name>
  <read_first>
    - src/components/features/export/export-dialog.tsx
    - src/types/report.ts
  </read_first>
  <files>src/components/features/export/export-dialog.tsx</files>
  <action>
    Add a new state variable inside ExportDialog: `const [selectedTemplate, setSelectedTemplate] = useState<'summary' | 'detailed'>('summary');`.
    Update the `handleExport` function to include the template property if the selected format is 'pdf':
    `template: selectedFormat === 'pdf' ? selectedTemplate : undefined`.
    In the JSX, below the format selection block and above the date range picker, add a conditional UI block:
    ```tsx
          {/* Template selection (PDF only) */}
          {selectedFormat === 'pdf' && (
            <div>
              <label className="text-sm font-medium mb-2 block">Report Template</label>
              <div className="flex gap-2">
                <Button
                  variant={selectedTemplate === 'summary' ? 'default' : 'outline'}
                  size="sm"
                  onClick={() => setSelectedTemplate('summary')}
                  className="font-mono text-xs uppercase"
                >
                  Summary
                </Button>
                <Button
                  variant={selectedTemplate === 'detailed' ? 'default' : 'outline'}
                  size="sm"
                  onClick={() => setSelectedTemplate('detailed')}
                  className="font-mono text-xs uppercase"
                >
                  Detailed
                </Button>
              </div>
            </div>
          )}
    ```
    Ensure Button classes follow the style defined previously for format buttons.
  </action>
  <acceptance_criteria>
    - `grep "selectedTemplate === 'summary' ? 'default' : 'outline'" src/components/features/export/export-dialog.tsx` returns a match
    - `grep "selectedFormat === 'pdf' && (" src/components/features/export/export-dialog.tsx` returns a match
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ExportDialog exposes template selector for PDF format</done>
</task>

<task type="auto">
  <name>Task 3: Update ReportsPage to pass template to exporter</name>
  <read_first>
    - src/app/(dashboard)/reports/page.tsx
    - src/components/features/export/export-dialog.tsx
  </read_first>
  <files>src/app/(dashboard)/reports/page.tsx</files>
  <action>
    In `src/app/(dashboard)/reports/page.tsx`:
    Update `handleExport` to use the template selected from options rather than hardcoding 'summary'. Change the hardcoded:
    `template: 'summary',` 
    to:
    `template: options.template || 'summary',`.
    Also update `handleQuickExport` to pass the correct template conditionally:
    `await handleExport({ format, dateRange, template: format === 'pdf' ? 'summary' : undefined });`
  </action>
  <acceptance_criteria>
    - `grep "template: options.template || 'summary'" src/app/(dashboard)/reports/page.tsx` returns a match
    - `grep "template: format === 'pdf' ? 'summary' : undefined" src/app/(dashboard)/reports/page.tsx` returns a match
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ReportsPage integrates template selection with PDF generation</done>
</task>

</tasks>

<verification>
Check that all types compile correctly and UI handles format selection without breaking existing date range or CSV export features.
</verification>

<success_criteria>
User can select either 'Summary' or 'Detailed' report templates in the UI when generating a PDF, closing the verification gap.
</success_criteria>

<output>
After completion, create `.planning/phases/04-reports-export/04-04-SUMMARY.md`
</output>
