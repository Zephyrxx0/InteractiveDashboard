---
phase: 04-reports-export
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - package.json
  - src/lib/export-pdf.ts
  - src/components/features/export/pdf-report-template.tsx
  - src/components/features/export/index.ts
autonomous: true
requirements: [RP-02, RP-03]

must_haves:
  truths:
    - "User can generate and download PDF reports"
    - "User can select from summary report templates"
    - "PDF contains formatted task data with styling"
  artifacts:
    - path: "src/lib/export-pdf.ts"
      provides: "PDF generation utilities"
      exports: ["exportTasksToPDF", "generatePDFReport"]
    - path: "src/components/features/export/pdf-report-template.tsx"
      provides: "PDF template components for react-pdf"
      exports: ["TaskReportDocument", "SummaryReportDocument"]
  key_links:
    - from: "src/lib/export-pdf.ts"
      to: "@react-pdf/renderer"
      via: "pdf() function"
      pattern: "react-pdf/renderer"
---

<objective>
Install @react-pdf/renderer and create PDF report generation with templates.

Purpose: Enable users to generate formatted PDF reports for sharing and printing (addresses Pitfall #5 by using client-side generation).
Output: PDF generation utilities with report templates, client-side rendering
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/04-reports-export/04-01-SUMMARY.md

<interfaces>
<!-- Export utilities from Plan 01 -->
From src/lib/export-utils.ts:
```typescript
export function downloadFile(content: string | Blob, filename: string, mimeType: string): void;
export function formatExportDate(date: Date | null | undefined): string;
export function generateFilename(prefix: string, extension: string): string;
```

From src/types/report.ts:
```typescript
export interface ReportConfig { ... }
export interface TaskReportData { ... }
```

From src/types/task.ts:
```typescript
export interface Task { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @react-pdf/renderer</name>
  <files>package.json</files>
  <action>
Install react-pdf for PDF generation:

```bash
npm install @react-pdf/renderer
```

Note: @react-pdf/renderer works client-side, avoiding server blocking (Pitfall #5).
  </action>
  <verify>
    <automated>npm ls @react-pdf/renderer</automated>
  </verify>
  <done>@react-pdf/renderer installed</done>
</task>

<task type="auto">
  <name>Task 2: Create PDF report templates</name>
  <files>src/components/features/export/pdf-report-template.tsx</files>
  <action>
Create react-pdf document templates:

```tsx
// src/components/features/export/pdf-report-template.tsx
'use client';

import { Document, Page, View, Text, StyleSheet } from '@react-pdf/renderer';
import { Task } from '@/types/task';
import { ReportConfig, TaskReportData } from '@/types/report';
import { formatExportDate } from '@/lib/export-utils';
import { format } from 'date-fns';

// PDF Styles
const styles = StyleSheet.create({
  page: {
    padding: 40,
    fontFamily: 'Helvetica',
    fontSize: 10,
  },
  header: {
    marginBottom: 20,
    borderBottomWidth: 1,
    borderBottomColor: '#e5e7eb',
    paddingBottom: 10,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#111827',
  },
  subtitle: {
    fontSize: 12,
    color: '#6b7280',
    marginTop: 4,
  },
  meta: {
    fontSize: 9,
    color: '#9ca3af',
    marginTop: 8,
  },
  section: {
    marginTop: 20,
  },
  sectionTitle: {
    fontSize: 14,
    fontWeight: 'bold',
    color: '#111827',
    marginBottom: 10,
  },
  table: {
    width: '100%',
  },
  tableHeader: {
    flexDirection: 'row',
    backgroundColor: '#f3f4f6',
    borderBottomWidth: 1,
    borderBottomColor: '#e5e7eb',
    padding: 8,
  },
  tableRow: {
    flexDirection: 'row',
    borderBottomWidth: 1,
    borderBottomColor: '#f3f4f6',
    padding: 8,
  },
  tableCell: {
    flex: 1,
  },
  tableCellHeader: {
    flex: 1,
    fontWeight: 'bold',
    fontSize: 9,
    textTransform: 'uppercase',
    color: '#6b7280',
  },
  summaryCard: {
    flexDirection: 'row',
    gap: 15,
    marginBottom: 20,
  },
  summaryItem: {
    flex: 1,
    backgroundColor: '#f9fafb',
    padding: 15,
    borderRadius: 4,
  },
  summaryValue: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#111827',
  },
  summaryLabel: {
    fontSize: 10,
    color: '#6b7280',
    marginTop: 4,
  },
  statusBadge: {
    paddingHorizontal: 6,
    paddingVertical: 2,
    borderRadius: 4,
    fontSize: 8,
  },
  footer: {
    position: 'absolute',
    bottom: 30,
    left: 40,
    right: 40,
    textAlign: 'center',
    fontSize: 9,
    color: '#9ca3af',
  },
});

const STATUS_COLORS: Record<string, string> = {
  'todo': '#6b7280',
  'in-progress': '#3b82f6',
  'done': '#10b981',
  'blocked': '#ef4444',
};

interface TaskReportDocumentProps {
  config: ReportConfig;
  data: TaskReportData;
}

/**
 * Summary Report Template - Overview with KPIs and task list
 */
export function SummaryReportDocument({ config, data }: TaskReportDocumentProps) {
  return (
    <Document>
      <Page size="A4" style={styles.page}>
        {/* Header */}
        <View style={styles.header}>
          <Text style={styles.title}>{config.title}</Text>
          {config.subtitle && <Text style={styles.subtitle}>{config.subtitle}</Text>}
          <Text style={styles.meta}>
            Generated: {format(config.generatedAt, 'MMMM d, yyyy h:mm a')}
            {config.dateRange && (
              ` • Data from ${format(config.dateRange.from, 'MMM d')} to ${format(config.dateRange.to, 'MMM d, yyyy')}`
            )}
          </Text>
        </View>

        {/* Summary Cards */}
        <View style={styles.summaryCard}>
          <View style={styles.summaryItem}>
            <Text style={styles.summaryValue}>{data.totalTasks}</Text>
            <Text style={styles.summaryLabel}>Total Tasks</Text>
          </View>
          <View style={styles.summaryItem}>
            <Text style={styles.summaryValue}>{data.completedTasks}</Text>
            <Text style={styles.summaryLabel}>Completed</Text>
          </View>
          <View style={styles.summaryItem}>
            <Text style={[styles.summaryValue, { color: '#ef4444' }]}>{data.overdueTasks}</Text>
            <Text style={styles.summaryLabel}>Overdue</Text>
          </View>
        </View>

        {/* Task Table */}
        <View style={styles.section}>
          <Text style={styles.sectionTitle}>Task Details</Text>
          <View style={styles.table}>
            <View style={styles.tableHeader}>
              <Text style={[styles.tableCellHeader, { flex: 2 }]}>Task</Text>
              <Text style={styles.tableCellHeader}>Status</Text>
              <Text style={styles.tableCellHeader}>Assignee</Text>
              <Text style={styles.tableCellHeader}>Due Date</Text>
            </View>
            {data.tasks.slice(0, 25).map((task, index) => (
              <View key={index} style={styles.tableRow}>
                <Text style={[styles.tableCell, { flex: 2 }]}>{task.name}</Text>
                <Text style={styles.tableCell}>{task.status}</Text>
                <Text style={styles.tableCell}>{task.assignee}</Text>
                <Text style={styles.tableCell}>{task.dueDate}</Text>
              </View>
            ))}
          </View>
          {data.tasks.length > 25 && (
            <Text style={{ marginTop: 10, color: '#6b7280', fontSize: 9 }}>
              ... and {data.tasks.length - 25} more tasks
            </Text>
          )}
        </View>

        {/* Footer */}
        <Text style={styles.footer}>
          Page 1 of 1 • Generated by Interactive Dashboard
        </Text>
      </Page>
    </Document>
  );
}

/**
 * Detailed Task Report Template - Full task list with all fields
 */
export function DetailedReportDocument({ config, data }: TaskReportDocumentProps) {
  const tasksPerPage = 20;
  const pageCount = Math.ceil(data.tasks.length / tasksPerPage);

  return (
    <Document>
      {Array.from({ length: pageCount }, (_, pageIndex) => {
        const pageTasks = data.tasks.slice(
          pageIndex * tasksPerPage,
          (pageIndex + 1) * tasksPerPage
        );

        return (
          <Page key={pageIndex} size="A4" style={styles.page}>
            {pageIndex === 0 && (
              <View style={styles.header}>
                <Text style={styles.title}>{config.title}</Text>
                <Text style={styles.meta}>
                  Generated: {format(config.generatedAt, 'MMMM d, yyyy')}
                </Text>
              </View>
            )}

            <View style={styles.table}>
              <View style={styles.tableHeader}>
                <Text style={[styles.tableCellHeader, { flex: 0.5 }]}>ID</Text>
                <Text style={[styles.tableCellHeader, { flex: 2 }]}>Task</Text>
                <Text style={styles.tableCellHeader}>Status</Text>
                <Text style={styles.tableCellHeader}>Assignee</Text>
                <Text style={styles.tableCellHeader}>Due</Text>
                <Text style={styles.tableCellHeader}>Created</Text>
              </View>
              {pageTasks.map((task, index) => (
                <View key={index} style={styles.tableRow}>
                  <Text style={[styles.tableCell, { flex: 0.5 }]}>{task.id}</Text>
                  <Text style={[styles.tableCell, { flex: 2 }]}>{task.name}</Text>
                  <Text style={styles.tableCell}>{task.status}</Text>
                  <Text style={styles.tableCell}>{task.assignee}</Text>
                  <Text style={styles.tableCell}>{task.dueDate}</Text>
                  <Text style={styles.tableCell}>{task.createdAt}</Text>
                </View>
              ))}
            </View>

            <Text style={styles.footer}>
              Page {pageIndex + 1} of {pageCount}
            </Text>
          </Page>
        );
      })}
    </Document>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/features/export/pdf-report-template.tsx</automated>
  </verify>
  <done>PDF report templates created with Summary and Detailed variants</done>
</task>

<task type="auto">
  <name>Task 3: Create PDF export utility</name>
  <files>src/lib/export-pdf.ts, src/components/features/export/index.ts</files>
  <action>
Create PDF generation utility:

```typescript
// src/lib/export-pdf.ts
'use client';

import { pdf } from '@react-pdf/renderer';
import { Task } from '@/types/task';
import { ReportConfig, TaskReportData, ExportOptions } from '@/types/report';
import { SummaryReportDocument, DetailedReportDocument } from '@/components/features/export/pdf-report-template';
import { downloadFile, formatExportDate, generateFilename } from './export-utils';
import { filterTasksByDateRange } from './export-csv';
import { isBefore } from 'date-fns';

type ReportTemplate = 'summary' | 'detailed';

/**
 * Prepare task data for PDF report.
 */
function prepareTaskReportData(tasks: Task[]): TaskReportData {
  const now = new Date();
  
  return {
    totalTasks: tasks.length,
    completedTasks: tasks.filter(t => t.status === 'done').length,
    overdueTasks: tasks.filter(t => 
      t.status !== 'done' && t.dueDate && isBefore(t.dueDate, now)
    ).length,
    tasksByStatus: tasks.reduce((acc, t) => {
      acc[t.status] = (acc[t.status] || 0) + 1;
      return acc;
    }, {} as Record<string, number>),
    tasksByAssignee: Object.entries(
      tasks.reduce((acc, t) => {
        const name = t.assignee?.name || 'Unassigned';
        acc[name] = (acc[name] || 0) + 1;
        return acc;
      }, {} as Record<string, number>)
    ).map(([name, count]) => ({ name, count })),
    tasks: tasks.map(t => ({
      id: t.id,
      name: t.name,
      status: t.status,
      assignee: t.assignee?.name || 'Unassigned',
      dueDate: formatExportDate(t.dueDate),
      createdAt: formatExportDate(t.createdAt),
    })),
  };
}

interface PDFExportOptions extends ExportOptions {
  template?: ReportTemplate;
  title?: string;
  subtitle?: string;
}

/**
 * Generate and download PDF report.
 * Runs client-side to avoid server blocking (Pitfall #5).
 */
export async function exportTasksToPDF(
  tasks: Task[],
  options: PDFExportOptions = { format: 'pdf' }
): Promise<void> {
  // Filter tasks if date range provided
  const filteredTasks = filterTasksByDateRange(tasks, options.dateRange);
  
  // Prepare report config
  const config: ReportConfig = {
    title: options.title || 'Task Report',
    subtitle: options.subtitle,
    generatedAt: new Date(),
    dateRange: options.dateRange,
  };

  // Prepare data
  const data = prepareTaskReportData(filteredTasks);

  // Select template
  const template = options.template || 'summary';
  const DocumentComponent = template === 'detailed' 
    ? DetailedReportDocument 
    : SummaryReportDocument;

  // Generate PDF blob (client-side)
  const blob = await pdf(<DocumentComponent config={config} data={data} />).toBlob();

  // Download
  const filename = options.filename || generateFilename('task_report', 'pdf');
  downloadFile(blob, filename, 'application/pdf');
}

/**
 * Generate PDF blob without downloading (for preview or API).
 */
export async function generatePDFReport(
  tasks: Task[],
  config: ReportConfig,
  template: ReportTemplate = 'summary'
): Promise<Blob> {
  const data = prepareTaskReportData(tasks);
  const DocumentComponent = template === 'detailed' 
    ? DetailedReportDocument 
    : SummaryReportDocument;
  
  return pdf(<DocumentComponent config={config} data={data} />).toBlob();
}
```

**Update index.ts:**
```typescript
export { ExportDialog } from './export-dialog';
export { SummaryReportDocument, DetailedReportDocument } from './pdf-report-template';
```
  </action>
  <verify>
    <automated>npx tsc --noEmit src/lib/export-pdf.ts && npm run build</automated>
  </verify>
  <done>PDF export utility with template selection created</done>
</task>

</tasks>

<verification>
1. @react-pdf/renderer in package.json
2. PDF templates render task data correctly
3. exportTasksToPDF generates downloadable PDF
4. Summary and Detailed templates available
5. Date range filtering works for PDF export
</verification>

<success_criteria>
- @react-pdf/renderer installed
- PDF templates with professional styling
- Summary report shows KPIs and task list
- Detailed report shows full task data
- PDF generates client-side (no server blocking)
</success_criteria>

<output>
After completion, create `.planning/phases/04-reports-export/04-02-SUMMARY.md`
</output>
