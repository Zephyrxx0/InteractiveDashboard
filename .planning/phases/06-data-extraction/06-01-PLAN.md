---
phase: 06-data-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DE-01, DE-02, DE-03, DE-04, DE-05, DE-06, DE-07, DE-08]
user_setup:
  - service: gemma4-local-api
    why: "Local LLM service for data extraction"
    env_vars:
      - name: GEMMA_API_URL
        source: "Local service endpoint (default: http://localhost:11434)"
    account_setup:
      - task: "Run Gemma4 Docker container"
        instructions: "docker run -d -p 11434:11434 --name gemma-api ollama/ollama:latest"
    dashboard_config: []
---

<objective>
Integrate Gemma4 Local LLM for extracting structured data from imported XLSX, DOCX, and CSV files. Users can import project files, trigger extraction via local API service, review results in editable table with confidence indicators, and save normalized data to project.
</objective>

<context>
@.planning/phases/06-data-extraction/06-CONTEXT.md
@.planning/phases/06-data-extraction/06-DISCUSSION-LOG.md
@src/components/data-import-modal.tsx
@src/lib/export-csv.ts
@src/app/(dashboard)/reports/page.tsx
</context>

<must_haves>
truths:
  - "User can import XLSX files and extract structured data rows"
  - "User can import DOCX files and extract text content"
  - "User can import CSV files and extract tabular data"
  - "Extracted data is presented in editable review table"
  - "Data is validated with confidence indicators"
  - "User can save extracted data to project"
  - "Processing happens asynchronously with progress indication"
  - "User can retry failed extractions"
artifacts:
  - path: "src/lib/extraction-api.ts"
    provides: "API client for local Gemma4 service"
    exports: ["extractFile", "pollExtractionStatus", "ExtractionJob", "ExtractionResult"]
  - path: "src/lib/parsers/xlsx-parser.ts"
    provides: "Deterministic XLSX parsing"
    exports: ["parseXLSX", "ParsedSpreadsheet"]
  - path: "src/lib/parsers/docx-parser.ts"
    provides: "Deterministic DOCX text extraction"
    exports: ["extractDOCXText", "DOCXContent"]
  - path: "src/lib/parsers/csv-parser.ts"
    provides: "CSV parsing"
    exports: ["parseCSV", "ParsedCSV"]
  - path: "src/lib/extraction-schema.ts"
    provides: "Hybrid canonical schema definitions"
    exports: ["ExtractionRecord", "ExtractionEnvelope", "confidenceThresholds"]
  - path: "src/components/features/extraction/extraction-review-table.tsx"
    provides: "Editable review table with confidence indicators"
    exports: ["ExtractionReviewTable"]
  - path: "src/components/features/extraction/extraction-progress.tsx"
    provides: "Async job progress UI"
    exports: ["ExtractionProgress"]
  - path: "src/components/features/extraction/extraction-modal.tsx"
    provides: "Per-project extraction modal"
    exports: ["ExtractionModal"]
key_links:
  - from: "src/components/features/extraction/extraction-modal.tsx"
    to: "src/lib/extraction-api.ts"
    via: "extractFile() call"
    pattern: "extractFile\\(projectId"
  - from: "src/components/features/extraction/extraction-review-table.tsx"
    to: "src/lib/extraction-schema.ts"
    via: "ExtractionRecord type"
    pattern: "ExtractionRecord\\[\\]"
  - from: "src/components/data-import-modal.tsx"
    to: "src/components/features/extraction/extraction-modal.tsx"
    via: "Reuse pattern"
    pattern: "data-import-modal.*evolved"
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Create extraction infrastructure types and API client</name>
  <files>src/lib/extraction-schema.ts, src/lib/extraction-api.ts</files>
  <action>
Create hybrid canonical schema (ExtractionRecord with core fields + source-specific extensions) and API client for local Gemma4 service.

- extraction-schema.ts: Define ExtractionEnvelope, ExtractionRecord, ConfidenceLevel enum, confidenceThresholds constants
- extraction-api.ts: Create extractFile(projectId, file, format), pollExtractionStatus(jobId), cancelExtraction(jobId) functions
- Use fetch API with timeout and retry logic
- API responds to GEMMA_API_URL env (default: http://localhost:11434)
- Returns async job with jobId for polling

DO NOT: Implement actual Gemma4 prompts - that's service-side. This is the Next.js client only.
  </action>
  <verify>
<automated>tsc --noEmit src/lib/extraction-schema.ts src/lib/extraction-api.ts</automated>
  </verify>
  <done>Extraction API client can submit files and poll job status</done>
</task>

<task type="auto">
  <name>Task 2: Create deterministic file parsers</name>
  <files>src/lib/parsers/xlsx-parser.ts, src/lib/parsers/docx-parser.ts, src/lib/parsers/csv-parser.ts</files>
  <action>
Create deterministic parsers for initial file processing before LLM enrichment.

- xlsx-parser.ts: Parse XLSX to 2D array with header detection using native XLSX library
- docx-parser.ts: Extract plain text from DOCX using mammoth.js or custom XML parsing
- csv-parser.ts: Parse CSV with proper handling of quotes, newlines, encoding

Each parser returns structured data and a "raw preview" for fallback display.
DO NOT: Call LLM here - just deterministic parsing.
  </action>
  <verify>
<automated>node -e "const {parseXLSX} = require('./src/lib/parsers/xlsx-parser'); console.log(typeof parseXLSX)"</automated>
  </verify>
  <done>Parsers can extract raw data from XLSX, DOCX, CSV files</done>
</task>

<task type="auto">
  <name>Task 3: Create extraction UI components</name>
  <files>src/components/features/extraction/extraction-modal.tsx, src/components/features/extraction/extraction-review-table.tsx, src/components/features/extraction/extraction-progress.tsx</files>
  <action>
Create three React components for the extraction flow.

- extraction-modal.tsx: Per-project modal triggered from import flow. Shows file selection, extraction options (format, schema mapping), runs extraction job
- extraction-progress.tsx: Shows job status (queued, parsing, extracting, enriching, complete, error) with progress percentage, elapsed time, cancel button
- extraction-review-table.tsx: Editable table showing ExtractionRecord[] with confidence indicators. Green=high, yellow=medium, red=low. Click cell to edit, tab to move between cells

Reuse patterns from data-import-modal.tsx (dialog structure, button variants).
Use extraction-schema.ts types directly.
Add "View raw parse" fallback link when confidence is low or extraction fails.
  </action>
  <verify>
<automated>tsc --noEmit src/components/features/extraction/*.tsx 2>&amp;1 | head -20</automated>
  </verify>
  <done>User sees extraction modal, progress during extraction, editable review table</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify end-to-end extraction flow</name>
  <what-built>Complete extraction flow from file import through review to save</what-built>
  <how-to-verify>
    1. Navigate to a project page (e.g., /projects/demo-project)
    2. Click import button, select an XLSX or DOCX file from feat_guide/
    3. Click "Extract with Gemma" - observe progress modal
    4. When complete, review extracted rows in editable table
    5. Edit one cell, tab to another
    6. Click "Save to Project" - confirm data appears in project data store
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] extraction-schema.ts exports ExtractionEnvelope with records array and metadata
- [ ] extraction-api.ts calls local service, handles async jobs
- [ ] All three parsers extract data from respective formats
- [ ] extraction-progress.tsx shows progress states
- [ ] extraction-review-table.tsx shows confidence colors and allows inline editing
- [ ] extraction-modal.tsx integrates all components
</verification>

<success_criteria>
1. User can select XLSX/DOCX/CSV file and trigger extraction
2. Progress modal shows extraction stages
3. Review table displays extracted data with confidence color coding
4. User can edit any extracted value inline
5. User can click "View raw parse" when extraction low-confidence
6. User can retry failed extraction
7. User can save extracted data to project
8. Processing runs via local API service (not cloud)
</success_criteria>

<output>
After completion, create `.planning/phases/06-data-extraction/06-01-SUMMARY.md`
</output>