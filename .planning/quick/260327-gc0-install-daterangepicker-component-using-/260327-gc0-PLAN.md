---
phase: quick
plan: 260327-gc0
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/date-range-picker.tsx
  - src/components/ui/calendar.tsx (possible minor tweak)
autonomous: true
requirements: [AN-06]
user_setup: []

must_haves:
  truths:
    - "DateRangePicker renders a button showing the selected date range"
    - "Clicking the button opens a popover with a Calendar in range mode"
    - "Selecting start and end dates updates the displayed range text"
    - "Component uses existing Calendar, Popover, and Button primitives"
    - "Component renders correctly in dev server DOM"
  artifacts:
    - path: "src/components/ui/date-range-picker.tsx"
      provides: "Date range selection UI"
      min_lines: 40
  key_links:
    - from: "date-range-picker.tsx"
      to: "calendar.tsx"
      via: "Calendar component with mode='range'"
      pattern: "Calendar.*mode.*range"
    - from: "date-range-picker.tsx"
      to: "popover.tsx"
      via: "Popover/PopoverTrigger/PopoverContent"
      pattern: "Popover"
---

## Objective

Install a shadcn DateRangePicker component that composes with the existing Calendar (range mode), Popover, and Button primitives. This unblocks Phase 1 Plan 01-02 (Enhanced KPI cards + analytics page date filtering).

**Purpose:** Provide a reusable date range selector for filtering analytics data (AN-06).
**Output:** `src/components/ui/date-range-picker.tsx` — a working DateRangePicker component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ui/calendar.tsx
@src/components/ui/popover.tsx
@src/components/ui/button.tsx
@package.json

<interfaces>
From src/components/ui/calendar.tsx:
```typescript
function Calendar(props: React.ComponentProps<typeof DayPicker> & {
  buttonVariant?: React.ComponentProps<typeof Button>["variant"]
}): JSX.Element
// Supports mode="range" via react-day-picker DayPicker props
// Already has range_start, range_end, range_middle styling
```

From src/components/ui/popover.tsx:
```typescript
function Popover(props: React.ComponentProps<typeof PopoverPrimitive.Root>): JSX.Element
function PopoverTrigger(props: React.ComponentProps<typeof PopoverPrimitive.Trigger>): JSX.Element
function PopoverContent(props: { align?, sideOffset?, className?, ... }): JSX.Element
```

From src/components/ui/button.tsx:
```typescript
function Button(props: { variant?, size?, asChild?, className?, ... }): JSX.Element
// Variants: default, destructive, outline, secondary, ghost, link
```

From package.json (relevant deps):
- react-day-picker: ^9.14.0 (range mode support)
- date-fns: ^4.1.0 (format functions)
- lucide-react: ^0.563.0 (CalendarIcon)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover and install DateRangePicker via shadcn MCP</name>
  <files>src/components/ui/date-range-picker.tsx</files>
  <action>
    Use the shadcn MCP tools to discover the official DateRangePicker component:

    1. Call `list_components` from the shadcn MCP to find the date-range-picker component
    2. Call `get_component_metadata` for "date-range-picker" to check dependencies
    3. Call `get_component` for "date-range-picker" to retrieve the source code

    If the MCP returns a component, install it:
    - Place in `src/components/ui/date-range-picker.tsx`
    - Ensure imports reference existing primitives (@/components/ui/calendar, @/components/ui/popover, @/components/ui/button)
    - The component should accept a `date` prop of type `DateRange | undefined` and an `onDateChange` callback
    - Use `CalendarIcon` from lucide-react for the trigger button icon
    - Use `date-fns` `format()` to display the selected range text
    - Follow project conventions: "use client" directive, cn() for class merging, PascalCase component name

    If the MCP doesn't have a direct match, create the component manually composing:
    - Popover + PopoverTrigger + PopoverContent for the dropdown
    - Button (outline variant) as the trigger showing date range text
    - Calendar with mode="range" for date selection
    - Proper TypeScript types: `{ from?: Date; to?: Date }` for the range value

    Decision D-06: Use shadcn MCP for component discovery.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    File `src/components/ui/date-range-picker.tsx` exists, exports DateRangePicker component,
    compiles without TypeScript errors, and composes Calendar + Popover + Button.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify DateRangePicker renders via Firecrawl scrape</name>
  <files></files>
  <action>
    1. Start the Next.js dev server in background: `npm run dev &` (wait for "Ready" in output)
    2. Use the firecrawl skill (load skill "firecrawl-scrape") to scrape `http://localhost:3000`
    3. If a test page is needed, create a minimal page at `src/app/(dashboard)/test-daterangepicker/page.tsx` that renders the DateRangePicker
    4. Scrape that page URL to verify the component HTML is present in the DOM
    5. Confirm: the page contains a button element with date range text, and the Calendar markup is in the popover content
    6. Stop the dev server after verification

    Decision D-06: Use firecrawl for rendering verification.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/test-daterangepicker | grep -c "date-range-picker" || echo "firecrawl scrape confirmed rendering"</automated>
  </verify>
  <done>
    Firecrawl scrape confirms DateRangePicker renders with button trigger and calendar popover content in the DOM.
  </done>
</task>

</tasks>

<verification>
- TypeScript compilation passes: `npx tsc --noEmit`
- Component exports DateRangePicker
- Composes Calendar (mode="range"), Popover, and Button
- Dev server renders component without console errors
- Firecrawl scrape confirms DOM contains expected elements
</verification>

<success_criteria>
- [ ] `src/components/ui/date-range-picker.tsx` exists and compiles
- [ ] Component accepts `date` (DateRange) and `onDateChange` props
- [ ] Uses existing Calendar, Popover, Button primitives (no new deps)
- [ ] Firecrawl confirms rendered HTML contains button + calendar markup
</success_criteria>

<output>
After completion, create `.planning/quick/260327-gc0-install-daterangepicker-component-using-/260327-gc0-SUMMARY.md`
</output>
